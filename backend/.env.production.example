# Production API — copy to `backend/.env.production` for local smoke tests only.
# On a real server, set these in the platform env (no file in repo).

APP_ENV=production
NODE_ENV=production

PORT=5000

DATABASE_URL="postgresql://user:pass@prod-db.example.com:5432/rentara?schema=public&sslmode=require"

CORS_ORIGIN=https://yourname.github.io,https://app.example.com
